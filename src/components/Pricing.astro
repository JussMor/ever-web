---
---

<section class="pricing-container">
  <div class="pricing-inner">
    <header class="pricing-header">
      <p class="eyebrow">Pricing Plans</p>
      <h2>Ready to Ignite Your Idea?</h2>
      <p class="sub">Choose the perfect package for your project and get started today</p>
    </header>

    <div class="cards">
      <article class="card main-card" aria-labelledby="plan-landing">
        <div class="card-top">
          <h3 id="plan-landing">Landing Page Design</h3>
          <p class="muted">Design + Framer build for high-converting startup pages</p>
        </div>

        <div class="price-row">
          <div class="price">$1997</div>
          <button class="btn btn-primary">Book a slot</button>
        </div>

        <ul class="features">
          <li>Launch-ready in 7 days</li>
          <li>Figma-to-Framer delivery</li>
          <li>Fully responsive layout</li>
        </ul>
      </article>

      <article class="card" aria-labelledby="plan-mvp">
        <div class="card-top">
          <h3 id="plan-mvp">MVP Development</h3>
          <p class="muted">Full-stack MVP builds for ambitious startups and founders</p>
        </div>

        <div class="price-row">
          <div class="price">$3497</div>
          <button class="btn btn-primary">Book a slot</button>
        </div>

        <ul class="features">
          <li>60 hours of development time per month</li>
          <li>Flexible hours allocation</li>
          <li>Weekly strategy calls</li>
        </ul>
      </article>
    </div>
  </div>
</section>

<style>
/* Container uses your global variables (do not hardcode theme colors here) */
.pricing-container {
  background: var(--bg);
  color: var(--text);
  padding: 4rem 1rem;
}

.pricing-inner {
  max-width: 1100px;
  margin: 0 auto;
}

.pricing-header { text-align: center; margin-bottom: 2.5rem; }
.pricing-header .eyebrow { color: var(--muted); font-size: .9rem; margin-bottom: .25rem; }
.pricing-header h2 { font-size: 2.25rem; margin: 0 0 .25rem; }
.pricing-header .sub { color: var(--muted); }

.cards { display: grid; gap: 1.5rem; grid-template-columns: 1fr; }
.card { background: var(--card-bg); border: 1px solid var(--border); border-radius: 14px; padding: 1.5rem; box-shadow: 0 6px 18px rgba(16,24,40,0.06); }

.card-top h3 { margin: 0 0 .25rem; }
.muted { color: var(--muted); }

.price-row { display:flex; align-items:center; justify-content:space-between; gap: 1rem; margin: 1rem 0; }
.price { font-size: 1.75rem; font-weight:700; }

.features { margin: 0; padding: 0; list-style: none; color: var(--muted); }
.features li { padding: .25rem 0; }

/* MAIN CARD: force black background and light text in both themes as requested */
.main-card {
  background: #000000 !important;
  color: #ffffff !important;
  border-color: rgba(0,0,0,0.25);
}

/* Ensure elements inside the forced black card look correct */
.main-card .muted { color: rgba(255,255,255,0.75); }
.main-card .price { color: #ffffff; }
.main-card .btn.btn-primary {
  /* keep the main CTA black as well so it matches the main card */
  background: #000000;
  color: #ffffff;
  border: 1px solid rgba(255,255,255,0.08);
}

/* Keep the default btn hover behavior but visible on black background */
.main-card .btn.btn-primary:hover {
  background: #ffffff;
  color: #000000;
}

@media (min-width: 768px) {
  .cards { grid-template-columns: repeat(2, 1fr); }
  .pricing-header h2 { font-size: 3rem; }
}
</style>
